# Spec

Superseded by the OpenSpec workflow. Start at `openspec/AGENTS.md`.

- Project context + locked decisions: `openspec/project.md`
- As-built kernel capability: `openspec/specs/layout-kernel/spec.md`
- Active change queue (implement in order): `openspec/changes/`
  1. `add-demo-gui`
  2. `add-gallery-strip` (depends on demo GUI)
  3. `add-container-queries`
  4. `add-archetypes`
