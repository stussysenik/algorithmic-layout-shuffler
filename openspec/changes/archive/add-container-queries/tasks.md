# Tasks

- [x] 1.1 Add `container-type: inline-size` to the shared section rule in `compile.ts`
- [x] 1.2 Add `text-wrap: pretty` to `.paragraph`; keep `balance` on `.headline`
- [x] 1.3 Add the `:has()` gap refinement for image-only sections
- [x] 1.4 Extend smoke test assertions (`container-type`, `text-wrap`)
- [x] 1.5 Verify: `npx tsc && node test/smoke.mjs` green; regenerate `samples/` and spot-check at 360px and 1440px widths
